export interface IItem {
    id?: string;
    name?: string; 
    description?: string; 
    categoryId?: string; 
    itemModelId?: string; 
    makeId?: string; 
    sellerId?: string; 
    imageUrl?: string; 
    quantity?: number; 
}